{{ 'component-account-menu.min.css' | asset_url | stylesheet_tag }}

<nav class="account-menu">
   <h1 class="account-menu__heading">{{ 'customer.account.title' | t }}</h1>
   
   <div class="account-menu__customer">
      <h6 class="account-menu__label">{{ 'customer.addresses.full_name' | t }}</h6>
      <div class="account-menu__name">{{ customer.name }}</div>
   </div>

   <ul class="account-menu__links">
      <li>{{ 'customer.menu_links.orders' | t | link_to: routes.account_url }}</li>
      <li>{{ 'customer.menu_links.addresses' | t | link_to: routes.account_addresses_url }}</li>
      <li>{{ 'customer.menu_links.wishlist' | t | link_to: routes.account_url }}</li>
      <li>{{ 'customer.menu_links.logout' | t | link_to: routes.account_logout_url }}</li>
   </ul>

   <div class="account-menu__links--mobile">
      <h6 class="account-menu__label">Menu</h6>
      <select class="link-selector link-selector--mobile js-selector">
         <option target="{{ routes.account_url }}">{{ 'customer.menu_links.orders' | t }}</li>
         <option target="{{ routes.account_addresses_url }}">{{ 'customer.menu_links.addresses' | t }}</li>
         <option target="{{ routes.account_url }}">{{ 'customer.menu_links.wishlist' | t }}</li>
         <option target="{{ routes.account_logout_url }}">{{ 'customer.menu_links.logout' | t }}</li>
       </select>
   </div>

    <style>
      .link-selector {
        background-image: url('{{ "icon-caret.svg" | asset_url }}');
      }
    </style>
</nav>